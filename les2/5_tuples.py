t = ('foo',)

#skal have komma da tuples bruges til at opbevare flere objekter
#uden komma ville det bare være et single objekt 

#This is also one of those cases where you can leave the parentheses off. 
# The trailing comma causes Python to assume a tuple:

t = 'foo',
t
('foo',)
type(t)

